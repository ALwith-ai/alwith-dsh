diff --git a/lib/index.js b/lib/index.js
index 9a68841c7f7d379fc8c78a4cca591c67ecd9d13d..82cd1f5d274234cf34ee4d502b57e1c0e1ea89a7 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -1,5 +1,13 @@
 import { launchEnvironmentOf } from "@deepseek-ai/dsh-launch-environment";
 import { CONTEXT_WINDOW_EXCEEDED_CODE, CallId, EMPTY_RESPONSE_CODE, INVALID_CREDENTIAL_CODE, LlmAdapter, LlmError, QUOTA_EXCEEDED_CODE, ReasoningEffortId, RetryPolicySchema, assertUsableApiKey, attributionHeaders, contentHasImage, isContextWindowExceededError, isQuotaExceededError, normalizeApiKey, offloadRequestImages, resolveRetryPolicy } from "@deepseek-ai/dsh-llm";
+// PATCHED for ALwith (upstream self-describes the gap as "absent for want of a
+// consumer"): an app-owned pi-ai CredentialStore reaches Models, so routes
+// declaring no apiKeyEnv authenticate through stored credentials — including
+// OAuth (Claude Pro etc.), which Models refreshes under the store lock.
+let __alwithCredentialStore;
+export function setCredentialStore(store) {
+	__alwithCredentialStore = store;
+}
 import { deepEqualJson, installSettingsSection, settingsNamespace } from "@deepseek-ai/dsh-settings";
 import { createModels, createProvider, getSupportedThinkingLevels, isContextOverflow } from "@earendil-works/pi-ai";
 import { MAX_TIMER_DELAY_MS, idleWatchdog, timeoutOf } from "@deepseek-ai/dsh-timeout";
@@ -780,7 +788,7 @@ var PiAiAdapter = class extends LlmAdapter {
 	current() {
 		const profiles = this.config.profiles();
 		if (this.snapshot?.profiles === profiles) return this.snapshot;
-		const models = createModels();
+		const models = createModels({ credentials: __alwithCredentialStore });
 		for (const profile of profiles.values()) models.setProvider(profile.piProvider);
 		this.snapshot = {
 			profiles,
